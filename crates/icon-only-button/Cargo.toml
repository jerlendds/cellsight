[package]
name = "cellsight-icon-only-button"
version = "0.1.0"
edition = "2024"

[dependencies]
cellsight-theme = { path = "../theme" }
gpui = { path = "../../zed/crates/gpui", default-features = false }
